server:
  port: 31004
spring:
  application:
    name: FEGIN-CLIENT
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
feign:
  hystrix:
    enabled: true # 开启熔断支持
management:
  endpoints:
    web:
      exposure:
        include: "*"
